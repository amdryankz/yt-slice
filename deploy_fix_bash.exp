#!/usr/bin/expect -f
set timeout -1
spawn ssh -p 22 root@213.136.64.179 "bash -lc 'cd /var/www/clipper && pnpm install && pnpm run build && pm2 restart clipper-web clipper-worker'"
expect {
    "yes/no" {
        send "yes\n"
        exp_continue
    }
    "password:" {
        send "3R5bdC0G\n"
    }
}
expect eof
